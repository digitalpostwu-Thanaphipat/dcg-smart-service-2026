# 📘 คู่มือการติดตั้งและตั้งค่าระบบหลังบ้าน (Deployment & Configuration Guide)
**โครงการ: DCG Smart Service (Version V2)**

คู่มือนี้สรุปขั้นตอนที่ผู้ดูแลระบบ (Admin) หรือนักพัฒนา ต้องดำเนินการเพื่อนำโค้ดระบบไปใช้งานจริงในสภาพแวดล้อมจริง (Production Environment) ทั้งฝั่ง Google Apps Script Backend และ React PWA Frontend

---

## 🛠️ ขั้นตอนที่ 1: ติดตั้งและอัปเดต Google Apps Script (Backend)

โค้ดควบคุมระบบหลังบ้านทั้งหมดอยู่ที่ไฟล์ [backend.gs](file:///D:/Dcg%20Smart%20Service/backend.gs)

### 1.1 การวางซอร์สโค้ด
1. เปิดสเปรดชีตสแตนดาร์ด **DCG Smart Service Database (V2)** ขึ้นมา
2. ไปที่เมนู **ส่วนขยาย (Extensions) > Apps Script**
3. ลบโค้ดเดิมในโปรเจกต์ออกทั้งหมด และคัดลอกโค้ดจากไฟล์ [backend.gs](file:///D:/Dcg%20Smart%20Service/backend.gs) รุ่นล่าสุดไปวางทับแทนที่
4. กดปุ่มบันทึกโครงการ (ไอคอนแผ่นดิสก์)

### 1.2 ตั้งค่าคุณสมบัติสคริปต์ (Script Properties)
ในหน้าโปรเจกต์ Apps Script ให้เข้าไปที่ **การตั้งค่าโครงการ (Project Settings)** (รูปไอคอนฟันเฟืองทางซ้ายมือ) จากนั้นเลื่อนลงมาที่หัวข้อ **คุณสมบัติสคริปต์ (Script Properties)** และเพิ่มค่าตัวแปรดังนี้:

| ชื่อคุณสมบัติ (Property Name) | ค่าที่ต้องกรอก (Value) | คำอธิบาย |
| :--- | :--- | :--- |
| `SPREADSHEET_ID` | *[ไอดีสเปรดชีต V2]* | รหัส ID ที่อยู่ใน URL ของไฟล์ Google Sheets ฐานข้อมูล |
| `LINE_NOTIFY_TOKEN` | *[รหัส Token ไลน์]* | ใช้สำหรับส่งข้อความแจ้งเตือนปัญหาระบบ / แจ้งเตือนบักระดับความรุนแรงสูง (High/Critical) |
| `ALLOW_MOCK_TOKEN` | `false` | ตั้งเป็น `false` เพื่อบังคับให้ผู้ใช้ทุกคนต้องล็อกอินด้วยรหัส OTP จริงเท่านั้น |

*หมายเหตุ: ไอดีสเปรดชีตสามารถคัดลอกได้จาก URL ของ Sheets เช่น `https://docs.google.com/spreadsheets/d/`**[ไอดีที่ต้องคัดลอก]**`/edit`*

### 1.3 เปิดสิทธิ์การเข้าใช้งานระบบแบคอัพและอีเมล (First-Time Authorization)
เพื่อให้ Apps Script ได้รับอนุญาตจากบัญชีผู้ดูแลระบบในการส่งอีเมล OTP และจัดการระบบไฟล์สำรองข้อมูลบน Google Drive:
1. ที่แถบด้านบนของตัวแก้ไขสคริปต์ ให้เลือกฟังก์ชัน `testSendEmail` จากรายการฟังก์ชัน
2. กดปุ่ม **เรียกใช้งาน (Run)**
3. จะมีป๊อปอัปให้ยืนยันสิทธิ์ (Authorization Required) ให้กด **ตรวจสอบสิทธิ์ (Review Permissions)**
4. เลือกบัญชี Google ของคุณ จากนั้นกด **ขั้นสูง (Advanced) > ไปที่ DCG Smart Service (ไม่ปลอดภัย)**
5. กด **อนุญาต (Allow)** เพื่อยืนยันให้สิทธิ์
6. ตรวจสอบในกล่องจดหมายอีเมลของคุณ จะต้องมีเมลทดสอบส่งเข้าไปเรียบร้อยแล้ว

### 1.4 การตั้งค่าระบบสำรองข้อมูลอัตโนมัติรายวัน (Daily Auto-Backup)
ระบบแบคอัพจะแปลงตารางธุรกรรมเป็นไฟล์ Excel และบันทึกลง Google Drive ในโฟลเดอร์ `Dcg Smart Service_Backup` และลบไฟล์สำรองที่เก่าเกิน 30 วันอัตโนมัติ
1. ที่ตัวแก้ไขสคริปต์ ให้เลือกฟังก์ชัน **`setupDailyBackupTrigger`**
2. กดปุ่ม **เรียกใช้งาน (Run)**
3. แถบ Logging จะแสดงข้อความการสร้าง Trigger รายวันสำเร็จ
*(การทำเช่นนี้จะสร้าง Time-Driven Trigger ให้ฟังก์ชันแบคอัพทำงานอัตโนมัติในช่วงเวลา 02.00 น. - 03.00 น. ของทุกวัน โดยที่คุณไม่ต้องเข้ามากดรันสคริปต์เองอีก)*

### 📌 1.4.1 ข้อสำคัญเกี่ยวกับข้อมูลใน Google Sheets และไฟล์แบคอัพใน Drive
เพื่อป้องกันความเข้าใจผิดของผู้ดูแลระบบ มีข้อมูลสำคัญที่ควรทราบดังนี้:
1. **ข้อมูลใน Google Sheets (ฐานข้อมูลหลัก)**: ข้อมูลธุรกรรมการบันทึกทั้งหมดในชีต (`Tx_InternalRun`, `Tx_InternalSort`, `Tx_ExternalPost`) **จะคงอยู่ถาวรตลอดไปและไม่มีการลบออกโดยอัตโนมัติ** ท่านสามารถสะสมข้อมูลเพื่อจัดทำสรุปรายงานสถิติย้อนหลังตลอดทั้งปีหรือข้ามปีงบประมาณได้ตลอดเวลา
2. **ไฟล์แบคอัพ Excel ใน Google Drive**: สิ่งที่ระบบจะทำการลบอัตโนมัติเมื่ออายุเกิน 30 วัน คือ **"ไฟล์แบคอัพสำรองรายวันฉบับ Excel (.xlsx)"** ที่บันทึกอยู่ในโฟลเดอร์ `Dcg Smart Service_Backup` เท่านั้น เนื่องจากระบบจะสร้างไฟล์ Excel ขึ้นมาใหม่ 1 ไฟล์ทุกวัน หากไม่มีระบบเคลียร์อัตโนมัติเมื่อครบ 30 วัน พื้นที่ Google Drive ของท่านจะเต็มอย่างรวดเร็ว (เพราะจะมีไฟล์แบคอัพเพิ่มขึ้นปีละ 365 ไฟล์) การลบนี้จึงไม่มีผลกระทบต่อฐานข้อมูลหลักใน Google Sheets แต่อย่างใด
3. **การดึงข้อมูลรายงานย้อนหลัง**: หากต้องการค้นหายอดการนำส่งภายนอกว่างบประมาณใช้ไปเท่าใดในรอบปีงบประมาณนี้ พนักงานสามารถคลิกไปที่เมนู **"รายงานผลการดำเนินงาน"** บนหน้าเว็บ แล้วเลือกช่วงเวลาตัวกรองเป็น **"ปีงบประมาณนี้"** (หรือกำหนดช่วงเวลาที่ต้องการเอง) และเลือกดูสถิติค่าใช้จ่ายในแท็บ **"นำส่งภายนอก"** ได้ทันทีโดยระบบจะคำนวณและสรุปยอดงบประมาณรวมจาก Google Sheets ให้ทันที

### 1.5 ทำการเผยแพร่เป็นเว็บแอป (Deploy Web App)
1. กดปุ่ม **การจัดใช้งานใหม่ (Deploy) > การจัดใช้งานใหม่ (New deployment)** ทางมุมขวาบน
2. เลือกประเภทการจัดใช้งานเป็น **เว็บแอป (Web app)** (หากยังไม่ได้เลือก)
3. ตั้งค่าพารามิเตอร์ดังนี้:
   - **คำอธิบาย (Description)**: `DCG Smart Service Production V2`
   - **เรียกใช้งานเป็น (Execute as)**: `ฉัน (อีเมลผู้ดูแลระบบของคุณ)`
   - **ผู้ที่มีสิทธิ์เข้าถึง (Who has access)**: `ทุกคน (Anyone)` *(สำคัญ: เพื่อให้เว็บแอปพลิเคชัน React ส่งคำขอ API มาหาหลังบ้านได้)*
4. กดปุ่ม **จัดใช้งาน (Deploy)**
5. ทำการคัดลอก **URL เว็บแอป (Web App URL)** ที่ได้มาเก็บไว้ (ลิงก์จะลงท้ายด้วย `/exec`)

---

## 💻 ขั้นตอนที่ 2: ตั้งค่าหน้าบ้านและการนำขึ้นระบบจริง (Frontend Hosting)

### 2.1 อัปเดตลิงก์ API
1. เปิดไฟล์ตั้งค่า [src/config.ts](file:///D:/Dcg%20Smart%20Service/src/config.ts) ในโปรเจกต์ React
2. แก้ไขค่าตัวแปร `API_URL` โดยนำ URL เว็บแอป (ที่ได้จากข้อ 1.5) ไปกรอกใส่แทนที่ค่าเดิม:
   ```typescript
   export const API_URL = 'https://script.google.com/macros/s/.../exec';
   ```
3. บันทึกไฟล์

### 2.2 คอมไพล์โปรเจกต์ใหม่
1. เปิดเทอร์มินัลในโฟลเดอร์โปรเจกต์ `D:\Dcg Smart Service`
2. รันคำสั่งคอมไพล์โปรดักชันบิลด์:
   ```bash
   npm run build
   ```
3. ระบบจะสร้างโฟลเดอร์ `dist` ซึ่งประกอบด้วยไฟล์ HTML, CSS, JavaScript และ Service Worker เวอร์ชันโปรดักชันที่พร้อมนำไปอัปลงโฮสติ้ง

### 2.3 ติดตั้งหน้าเว็บจริงบน Production Hosting (เช่น Vercel)
คุณสามารถนำโฟลเดอร์โปรเจกต์นี้อัปขึ้น GitHub จากนั้นเชื่อมต่อไปยังโฮสติ้งฟรีระดับองค์กรอย่าง Vercel ได้อย่างรวดเร็ว:
1. เชื่อมต่อ Repository บน Git ของคุณ
2. ใน Vercel Dashboard ให้กด **Add New > Project** และเลือกโปรเจกต์นี้
3. ตั้งค่า Build Command เป็น `npm run build` และ Output Directory เป็น `dist`
4. กด **Deploy** เพื่อเปิดใช้งานระบบจริงผ่านอินเทอร์เน็ต

---

## 🔍 ขั้นตอนที่ 3: ตรวจสอบความถูกต้องหลังการติดตั้ง (Smoke Testing)
หลังจากดำเนินการข้างต้นเสร็จสิ้น ให้เปิดเว็บแอปพลิเคชันหน้าจริงและตรวจสอบเงื่อนไขดังต่อไปนี้:
1. **ล็อกอินผู้ใช้งานจริง**: ทดลองส่งอีเมล OTP ไปยังอีเมล `@wu.ac.th` ที่มีรายชื่ออยู่ในสิทธิ์ `Master_Users` และนำรหัส 6 หลักมากรอกล็อกอิน ต้องผ่านสำเร็จ
2. **การทำธุรกรรมจริง**: ทดลองกดบันทึกข้อมูล "รับ-ส่งภายใน" หรือ "นำส่งภายนอก" 1 รายการ แล้วตรวจเช็คที่ Google Sheets ว่าข้อมูลถูกเขียนลงแถวล่าสุดอย่างถูกต้อง
3. **ตรวจสอบเวลา (Timestamp)**: เช็คใน Google Sheets ว่าคอลัมน์ Timestamp มีข้อมูลทั้ง **วันที่ และ เวลา** แสดงอย่างชัดเจน (เช่น `2026-06-08 10:47:00`)
4. **ตรวจสอบการแบคอัพ**: ตรวจดูใน Google Drive ว่ามีโฟลเดอร์ `Dcg Smart Service_Backup` ปรากฏขึ้นมาอย่างถูกต้อง
5. **ทดสอบคัดลอกสรุปสำหรับ LINE**: ลองกดรายงานสรุปในหน้า `ReportPage` และกดคัดลอกสรุป ข้อความที่คัดลอกมาควรแสดงวันที่จริงในรูปแบบภาษาไทย เช่น `(วันที่ 8 มิถุนายน 2569)` แทนคำว่า `(วันนี้)`
